notepad .windsurfrules
```

Remplace tout par :
```
# ========================================
# RÈGLES DE DÉPLOIEMENT VPS - ZETA AI
# ========================================

## ARCHITECTURE
- Backend : VPS Contabo (194.60.201.228) via Docker
- Frontend : Vercel (déploiement automatique depuis zeta-ai-vercel/)
- Base de données : Supabase + Meilisearch

## COMMANDE DE DÉPLOIEMENT BACKEND
Pour toute modification backend, utiliser UNIQUEMENT :
.\deploy.ps1 "description de la modification"

Le script fait automatiquement :
1. Vérifie si des fichiers dangereux sont modifiés (Dockerfile, docker-compose.yml, requirements.txt)
2. Affiche la liste des fichiers qui seront déployés
3. git add + commit + push GitHub
4. git pull sur le VPS
5. Vérifie l'état du VPS après déploiement

## EN CAS DE PROBLÈME → ROLLBACK IMMÉDIAT
.\deploy.ps1 -rollback

Le rollback annule le dernier commit sur le VPS automatiquement.
NE PAS hésiter à rollback si le backend ne répond plus.

## ⚠️ POURQUOI CES RÈGLES SONT CRITIQUES
Le backend tourne en production sur Docker sur un VPS.
Une mauvaise modification peut :
- Casser le Docker et mettre le backend hors ligne
- Affecter les vrais utilisateurs de l'application
- Nécessiter une intervention manuelle longue pour réparer
- Risquer de perdre des données si les volumes Docker sont touchés
Cela s'est déjà produit 2 fois : le backend a été cassé et il a fallu
rollback sur une version datant d'une semaine pour récupérer.

## ⛔ CE QUE L'IA NE DOIT JAMAIS FAIRE
- NE JAMAIS modifier Dockerfile sans validation humaine explicite
- NE JAMAIS modifier docker-compose.yml sans validation humaine explicite
- NE JAMAIS lancer docker compose build sans autorisation explicite
- NE JAMAIS lancer docker compose down sans autorisation explicite
- NE JAMAIS faire git push direct sur le VPS
- NE JAMAIS modifier les fichiers .env du VPS
- NE JAMAIS supprimer des volumes Docker
- NE JAMAIS inclure les dossiers frontend dans un commit backend

## ✅ CE QUE L'IA PEUT FAIRE
- Modifier les fichiers Python du backend (.py)
- Modifier requirements.txt MAIS avertir que rebuild Docker sera nécessaire
- Modifier les fichiers de config non-Docker
- Lancer .\deploy.ps1 pour déployer
- Lancer .\deploy.ps1 -rollback en cas de problème

## FICHIERS SENSIBLES (alerte automatique dans deploy.ps1)
Si ces fichiers sont modifiés, deploy.ps1 demandera confirmation :
- Dockerfile → rebuild Docker obligatoire après
- docker-compose.yml → rebuild Docker obligatoire après
- requirements.txt → rebuild Docker obligatoire après
- .env → ne jamais committer

## REBUILD DOCKER (manuel, humain uniquement)
Uniquement si Dockerfile ou requirements.txt modifiés, sur le VPS :
docker compose build zeta-backend
docker compose up -d
docker compose ps
curl http://localhost:8002/ingestion/health

## VÉRIFICATION ÉTAT VPS (sans risque)
git fetch origin
git diff HEAD origin/main -- Dockerfile
git diff HEAD origin/main -- docker-compose.yml
docker compose ps
curl http://localhost:8002/ingestion/health
docker inspect zeta-backend | grep -A5 "Cmd"

## RÉFÉRENCE ABSOLUE
Le VPS qui tourne = la vérité terrain.
En cas de doute entre local et VPS → toujours s'aligner sur le VPS.
Ne jamais écraser la config du VPS avec celle du local sans vérification.

## STRUCTURE DES DOSSIERS
- Backend (VPS + GitHub) : tout le code Python et config
- Frontend Vercel : zeta-ai-vercel/ → NE PAS inclure dans commits backend
- Archive frontend : frontend/ → NE PAS inclure dans commits backend
- Ignorés : results/, tests/reports/, .venv/, *.tar.gz, *.pt, *.pkl